import uploadImage from "@/modules/daybook/helpers/uploadImage";
import axios from "axios";

desc('pruebas en el upload image', () => {
    test('debe cargar archivo y retornar url', async() => {

    });
});